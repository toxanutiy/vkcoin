import { Middleware, MiddlewarePayload } from './types';
export declare function compose<T>(middlewares: Middleware<T>[]): Middleware<T>;
export declare function composeWithPayload<T, P>(middlewares: MiddlewarePayload<T, P>[], next: (context: T, payload: P) => Promise<any>): (context: T, payload: P) => Promise<P>;
